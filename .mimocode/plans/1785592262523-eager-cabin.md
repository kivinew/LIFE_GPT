# Plan: Simplify Division Conditions & Optimize `divide()`

## Context

The current `can_divide()` in `cell.py` checks 5 conditions (energy, age, age ratio, temperature, food). The user wants to simplify to just two: energy > 95% of max for current level, and minimum age 1000 ticks. Also optimize the `divide()` function by removing redundant code.

## Changes

### 1. `config.py` — Update division constants
- Change `DIVIDE_MIN_AGE = 10` → `DIVIDE_MIN_AGE = 1000`
- Add `DIVIDE_ENERGY_RATIO = 0.95` (new constant for 95% energy threshold)
- Remove unused constants: `DIVIDE_THRESHOLD`, `DIVIDE_MIN_TEMP`, `DIVIDE_MAX_TEMP`, `DIVIDE_MIN_FOOD`, `DIVIDE_MAX_AGE_RATIO`

### 2. `cell.py` — Simplify `can_divide()` and optimize `divide()`

**`can_divide()` (line 912):**
- Remove `field` parameter (no longer needed — no temperature/food checks)
- Replace body with two checks only:
  - `self.energy < self.max_energy * DIVIDE_ENERGY_RATIO` → return False
  - `self.age < DIVIDE_MIN_AGE` → return False
- Remove temperature, age ratio, and food checks entirely

**`divide()` (line 949):**
- Remove duplicate energy check at line 967 (`if self.energy < max_e * DIVIDE_THRESHOLD`) — `can_divide` already guarantees this
- Remove `max_e` recalculation at line 956 — use `self.max_energy` instead (already a property)
- Keep: zoo_elite logic, child energy minimum check, speciation block, level/age reset, sound

**Imports (lines 8-80):**
- Remove: `DIVIDE_THRESHOLD`, `DIVIDE_MIN_TEMP`, `DIVIDE_MAX_TEMP`, `DIVIDE_MIN_FOOD`, `DIVIDE_MAX_AGE_RATIO`
- Add: `DIVIDE_ENERGY_RATIO`
- Keep: `DIVIDE_MIN_AGE`

### 3. `main.py` — Remove unused import
- Remove `DIVIDE_THRESHOLD` from config imports (line 58) — no longer used anywhere in main.py

### 4. `AGENTS.md` — Update division documentation
- Update the "Division" entry to reflect the simplified conditions

## Verification

1. Run `python main.py` — verify the sim starts and cells divide correctly
2. Test that cells only divide when energy > 95% max and age >= 1000
3. Verify no import errors or NameError exceptions
4. Check that death sounds still work (no regression from recent audio changes)
